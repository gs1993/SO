import { PostDetailsPipe } from './post-details.pipe';

describe('PostDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new PostDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
